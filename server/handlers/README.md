# Handler Layer

This layer is reserved for network handler modules:

- REST API routes
- webhook endpoints
- SSE endpoints

Implementation will progressively move route logic out of server/index.ts.
