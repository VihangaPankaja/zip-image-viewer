# Domain Layer

This folder will hold strongly typed domain models and contracts.

Initial modularization keeps runtime behavior unchanged while preparing
for dependency-injected handlers and services.
